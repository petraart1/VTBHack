name: Build Mobile App

on:
  push:
    paths:
      - 'mobile/**'
      - '.github/workflows/mobile-ci.yaml'
    branches:
      - main
  pull_request:
    paths:
      - 'mobile/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./mobile/Dockerfile
          push: false
          load: true
          tags: android-builder:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build APK
        run: |
          mkdir -p ${{ github.workspace }}/output
          docker run --rm \
            -v ${{ github.workspace }}/mobile:/app/mobile \
            -v gradle_cache:/app/.gradle \
            -v ${{ github.workspace }}/output:/output \
            android-builder:latest

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: debug-apk
          path: ${{ github.workspace }}/output/app-debug.apk
          retention-days: 5

      - name: Create Release on tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ github.workspace }}/output/app-debug.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
