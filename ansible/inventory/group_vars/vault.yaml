---
# Vault-protected secrets
# Зашифрован через: ansible-vault encrypt --vault-password-file .vault_pass vault.yaml
# Расшифровка: ansible-vault view --vault-password-file .vault_pass vault.yaml

vault_postgres_password: "changeme_strong_password"
vault_jwt_secret: "changeme_jwt_secret_here"
vault_redis_password: ""
